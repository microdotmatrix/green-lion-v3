---
import "@/styles/global.css";

import { AdminServicesPage } from "@/components/admin/pages/admin-services-page";
import { TailwindIndicator } from "@/components/elements/tailwind-indicator.tsx";
import Meta from "@/components/layout/meta.astro";
import { SITE } from "@/lib/config";

const user = Astro.locals.user;
const session = Astro.locals.session;

if (!user || !session) {
  return Astro.redirect("/signin?redirect=/admin/services");
}
---

<!doctype html>
<html lang="en" class="dark">
  <Meta
    title={`Services | Admin | ${SITE.title}`}
    description="Manage Services"
  />
  <body class="bg-zinc-950 text-zinc-50 antialiased">
    <AdminServicesPage client:only="react" user={user} />
    {import.meta.env.DEV && <TailwindIndicator client:load />}
  </body>
</html>
